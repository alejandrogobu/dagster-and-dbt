version: 2

sources:
  - name: chicago_crimes
    schema: chicago_crimes
    tables:
      - name: crimes
        meta:
          dagster:
            asset_key: ["dlt_chicago_crimes_source_get_crimes"] # This metadata specifies the corresponding Dagster asset for this dbt source.
        columns:
          - name: id
            data_type: varchar
            description: Unique identifier for the crime report.
            data_tests:
              - not_null