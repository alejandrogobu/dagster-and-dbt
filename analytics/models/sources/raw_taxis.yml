version: 2

sources:
  - name: raw_taxis
    schema: main
    tables:
      - name: zones
        meta:
          dagster:
            asset_key: ["taxi_zones"] # This metadata specifies the corresponding Dagster asset for this dbt source.
      - name: trips
        meta:
          dagster:
            asset_key: ["taxi_trips"] # This metadata specifies the corresponding Dagster asset for this dbt source.